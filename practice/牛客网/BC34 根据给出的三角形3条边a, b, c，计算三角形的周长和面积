#include <stdio.h>
#include <math.h>
int main()
{
    float a,b,c;
    scanf("%f %f %f",&a,&b,&c);
    float p = (a+b+c)/2;
    float s = sqrt(p*(p-a)*(p-b)*(p-c));
    printf("circumference=%.2f area=%.2f",a+b+c,s);
    
    return 0;
}
